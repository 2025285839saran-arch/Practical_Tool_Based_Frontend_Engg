This webpage uses basic HTML structure with one heading and two paragraphs. 
CSS is applied using the internal <style> tag to center the heading and set the paragraph color to dark gray.
The code demonstrates simple use of HTML tags and basic styling. It fulfills all requirements of the task.
